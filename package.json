{
  "name": "@dschnare/chai",
  "version": "1.0.2",
  "bin": {
    "chai": "./bin/chai.js"
  },
  "description": "A crawler that grabs the URL, page titles, and first H1 and H2 elements.",
  "main": "index.js",
  "scripts": {
    "test": "eslint index.js test/specs lib bin && istanbul cover test/specs/crawl.js && tape test/specs/**.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^0.19.0",
    "superagent": "^1.4.0"
  },
  "devDependencies": {
    "codeclimate-test-reporter": "^0.1.1",
    "eslint": "^1.5.0",
    "istanbul": "^0.3.20",
    "tape": "^4.2.0"
  },
  "engines": {
    "node": ">=0.10 <=0.12"
  }
}
